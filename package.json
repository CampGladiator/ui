{
  "name": "CGUI",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/CampGladiator/CGUI.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/CampGladiator/CGUI/issues"
  },
  "devDependencies": {
    "@babel/core": "^7.3.4",
    "@storybook/addon-actions": "^5.0.3",
    "@storybook/addon-centered": "^5.0.3",
    "@storybook/addon-info": "^5.0.3",
    "@storybook/addon-links": "^5.0.3",
    "@storybook/addon-viewport": "^5.0.3",
    "@storybook/addons": "^5.0.3",
    "@storybook/react": "^5.0.3",
    "autoprefixer": "^9.4.7",
    "babel-loader": "^8.0.5",
    "clean-css-cli": "^4.2.1",
    "concurrently": "^4.1.0",
    "node-sass": "^4.11.0",
    "postcss-cli": "^6.1.1",
    "prettier": "^1.16.4",
    "react-storybook-decorator-background": "^1.0.5",
    "rimraf": "^2.6.3",
    "sass-lint": "^1.12.1"
  },
  "scripts": {
    "build": "npm run build:clean && npm run build:sass && npm run build:autoprefix && npm run build:cleancss",
    "build:autoprefix": "postcss --use autoprefixer --map false --output build/main.css build/main.css",
    "build:cleancss": "cleancss -o build/main.min.css build/main.css",
    "build:clean": "rimraf css",
    "build:sass": "node-sass --error-bell --output-style expanded --source-map true main.sass build/main.css",
    "build:watch": "npm run build:sass -- --watch",
    "deploy": "npm run build",
    "start": "concurrently \"yarn run storybook\" \"yarn run build:watch\"",
    "lint": "sass-lint sass/**/*.sass -v",
    "storybook": "start-storybook -p 6006",
    "storybook:build": "build-storybook"
  },
  "prettier": {
    "singleQuote": true,
    "semi": false,
    "trailingComma": "all"
  },
  "homepage": "https://github.com/CampGladiator/CGUI#readme",
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
